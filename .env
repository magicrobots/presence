# Vite environment variables
# VITE_BUILD_NUMBER is shown in the about/version commands as the patch version (v1.4.N)
VITE_BUILD_NUMBER=1

# AWS SES credentials for cmd-contact (commented out — contact is disabled in the registry)
# VITE_AWS_ID=your_aws_access_key_id_here
# VITE_AWS_ACCESS=your_aws_secret_access_key_here
